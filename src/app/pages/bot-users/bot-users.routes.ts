import { Route } from '@angular/router';
import { BotUsersComponent } from './bot-users.component';

const routes: Route[] = [
  {
    path: '',
    component: BotUsersComponent,
  },
];

export default routes;
