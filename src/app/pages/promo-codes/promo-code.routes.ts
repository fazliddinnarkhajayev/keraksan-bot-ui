import { Route } from '@angular/router';
import { PromoCodesComponent } from './promo-code.component';

const routes: Route[] = [
  {
    path: '',
    component: PromoCodesComponent,
  },
];

export default routes;
